---
name: remotion-ref-replay
description: 已并入 brand-video(兼容别名)。参考录屏→真机捕获→Remotion 重建的方法(storyboard 纪律、捕获管线、舞台相机权重算法、speed cut)现在是 brand-video 的分析+捕获模块。触发本 skill 时直接加载 brand-video 及其 references/replay-capture.md。
metadata:
  internal: true
---

# remotion-ref-replay(已并入 brand-video)

本 skill 的全部正文已迁至
[`../brand-video/references/replay-capture.md`](../brand-video/references/replay-capture.md),
作为 brand-video(brand-studio 的通用 video producer)的分析+捕获模块统一迭代——
不再单独维护两份重叠的 skill。

读这个名字的旧引用(memory、docs、README)仍然有效:按 brand-video 的流程走,
replay 相关的方法、相机权重算法、排错表都在上面那份 reference 里,一字未删。
