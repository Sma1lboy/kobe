---
"@sma1lboy/kobe": patch
---

feat: image/binary files in the ops preview render a metadata card (type + size) instead of mojibake; `o` opens the file in the system viewer (macOS `open` / Linux `xdg-open` / Windows `start`) for local worktrees.
