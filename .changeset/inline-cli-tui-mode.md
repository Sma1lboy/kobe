---
"@sma1lboy/kobe": patch
---

CLI-command TUIs now render inline (ink-style) in a small main-screen footer instead of taking over the alternate screen — your shell scrollback stays visible. `kobe update list` is the first inline page.
