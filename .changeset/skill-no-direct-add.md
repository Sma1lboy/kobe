---
"@sma1lboy/rove": patch
---

Rove skill v28: sharpen the cross-repo request protocol — send the report to the target project's main task, never `add` a task into someone else's repo. The reporter holds problem-side context only; the receiving main agent holds solution-side context and owns decomposing the report into tasks.
