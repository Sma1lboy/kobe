---
"@sma1lboy/kobe": patch
---

fix: the sidebar project filter now scopes the PROJECTS list too, so the header's all/project label matches what is shown; the header stays visible while a filter is active
