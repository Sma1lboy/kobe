---
"@sma1lboy/kobe": patch
---

Make the keyboard model discoverable: the prefix now opens a context-aware which-key command map with a five-second selection window, while F1 leads with current-pane, one-press, and prefix sections. Both views follow live rebindings while preserving terminal passthrough, and missing localized action labels are filled in.
