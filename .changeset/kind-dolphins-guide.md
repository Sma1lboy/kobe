---
"@sma1lboy/kobe": patch
---

Make the keyboard model discoverable: the prefix now opens a context-aware which-key command map, F1 leads with current-pane/one-press/prefix sections, and a one-time three-step coach teaches the grammar through real actions. The Settings keybinding page and docs use the same vocabulary, the default second-key window is three seconds, and missing localized action labels are filled in.
