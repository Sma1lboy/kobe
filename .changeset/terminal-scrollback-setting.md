---
"@sma1lboy/kobe": patch
---

Terminal scrollback grows from 200 to 1000 rows by default, and is now configurable in Settings → General → Terminal (100–100000 rows). Applies to terminals opened after the change.
