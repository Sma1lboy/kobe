---
"@sma1lboy/kobe": patch
---

Refreshes the README hero screenshot and demo video against the current TUI, and repairs the capture pipeline that produces them so it runs off macOS. The replay now drives the BUILT cli, so the fan-out prompt coda in the recording reads the `kobe api …` an installed user sees instead of the capture host's absolute bun and repo paths; the shell prompt is pinned by the spec (`capture.shellPrompt`, exported as PS1) rather than inherited from whoever runs it; the demo engine stub uses POSIX octal escapes, since dash's `printf` prints `\xHH` literally and silently broke the `ready ›` wait marker; and the sidecar no longer aborts on Linux, where node-pty ships no prebuilt `spawn-helper` to chmod. The storyboard's stale waits and focus are fixed too: `focusPaneBeforeOpen` sends the `ctrl+a` `h` prefix sequence (a fresh boot does not focus the sidebar, so the bare `n` that opens the dialog was being swallowed), the new-task wait uses a dialog-only label instead of one the sidebar's own button matched, and the engine picker waits on its current "New conversation" title. `visual:shot` gains `--scale` (device pixel ratio for 2× docs assets) and a `wait:<ms>` token.
