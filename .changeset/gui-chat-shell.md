---
"@sma1lboy/kobe": patch
---

kobe-web: /chat — a kaku-style GUI chat shell experiment. Left session rail, center structured conversation (engine history) with a GUI prompt composer, right session-info panel; a collapsible terminal drawer attaches the real PTY and auto-opens when the engine waits on input.
