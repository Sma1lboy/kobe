---
"@sma1lboy/kobe": patch
---

Settings → General → Terminal now states what a larger scrollback costs: redraw CPU scales with the setting, and 1000 rows is roughly 2× the cost of 50. The number was previously invisible, so raising it looked free.
