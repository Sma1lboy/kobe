---
"@sma1lboy/kobe": patch
---

Internal: cover `engineLabel` (vendor id → display label across New Task / Settings / workspace pickers) — known id → label, unknown id → raw id, missing id → claude default, empty list. 111 web tests, completing coverage of the package's pure helpers.
