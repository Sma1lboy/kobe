---
"@sma1lboy/kobe": patch
---

Chat tab strip now shows even when a task has a single tab, so the engine title and turn chip are always visible. Settings → General → Terminal has a new "Hide the tab strip when a chat has one tab" toggle to restore the old behavior.
