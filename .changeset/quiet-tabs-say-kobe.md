---
"@sma1lboy/kobe": patch
---

Launching the TUI now labels the outer terminal tab/window `kobe` instead of letting iTerm2 fall back to the JavaScript runtime name (`node`). Redirected CLI output remains free of terminal control sequences.
