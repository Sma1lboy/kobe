---
"@sma1lboy/kobe": patch
---

`kobe api notify` is now discoverable: listed in the agent skill (SKILL v12) and grouped under `drive` in the schema index instead of falling through to `other` — agents can find the toast channel without spelunking the full spec.
