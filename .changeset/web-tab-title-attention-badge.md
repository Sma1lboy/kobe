---
"@sma1lboy/kobe": patch
---

The browser tab title now shows a "(N) kobe" attention badge when N tasks need a human (waiting on input, errored, or rate-limited) — so a backgrounded dashboard tab tells you at a glance how many sessions want you, the visual complement to the desktop notifications that works even without notification permission. The count is the same "Needs you" set the board surfaces (shared `triage`), updates live on every route, and drops back to a bare "kobe" when nothing is waiting.
