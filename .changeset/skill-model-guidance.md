---
"@sma1lboy/rove": patch
---

Rove skill v25: composing `--command` now treats the model as a conscious either/or — omit any model flag by default (the user's engine default is fresher than the agent's training-data model list), pin one only when the user named it this turn, passing their string verbatim. Also repairs a truncated sentence in the parallel-round rules.
