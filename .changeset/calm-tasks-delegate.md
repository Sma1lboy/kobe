---
"@sma1lboy/kobe": patch
"@sma1lboy/kobe-daemon": patch
---

Add directed primary-to-subagent Task links through `prefix+@`, with no chat fork or shared Channel UI. The selected Task is persisted as a subagent and both agents receive an explicit `kobe api` delegation protocol through the primary's bootstrap turn and the Kobe skill.
