---
"@sma1lboy/kobe": patch
---

CLI ergonomics: `kobe update list` / `kobe update dry-run` verbs are now the canonical spellings (`--list`/`--dry-run` stay as aliases), and `kobe completions zsh` output now works both as an fpath autoload file and via `source <(kobe completions zsh)` — with real install instructions in `kobe completions --help`.
