---
"@sma1lboy/kobe": patch
---

Add the local `kobe web` dashboard with task selection, independent vendor and terminal tabs, notes, worktree changes, centered file previews, and bundled Nerd Font terminal rendering. The production `kobe web` command now ships the built SPA and starts the Node PTY server alongside the daemon web transport, so terminal-backed tabs work outside the Vite dev launcher.
