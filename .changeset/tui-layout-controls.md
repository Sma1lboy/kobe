---
"@sma1lboy/kobe": patch
---

TUI task sessions now expose tmux-native layout controls: temporary middle workspace shell splits capped at four panes, Tasks pane hide/restore, file/Ops pane hide/show, and terminal hide/restore by moving panes to a background tmux window without killing their processes.
