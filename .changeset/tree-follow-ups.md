---
"@sma1lboy/kobe": patch
---

Tree sidebar follow-ups from live use: `kobe .` directory tasks group under their own directory header instead of visually attaching to the previous project; walking the tree no longer snaps the cursor back to the selected row every poll tick; and a completed turn's badge is now just the unseen ● lamp — once you've looked at the session it returns to the idle circle instead of wearing a ✓ forever.
