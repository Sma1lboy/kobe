---
"@sma1lboy/kobe": patch
---

Web issue starts now read as user stories that spawn kobe sessions, and the web bridge's canonical tmux session path runs the same repo init prompt contract as TUI entry. Web PTY issue prompts still suppress the repo init prompt so explicit story instructions are not duplicated.
