---
"@sma1lboy/kobe": patch
---

**The task sidebar header gains a `+` new-task button, and the version now sits next to the KOBE name.** The brand header used to push the version chip to the far-right edge; it now clusters on the left as `KOBE  v0.x.x  +`, with a clickable `+` that runs the same create flow as the `n` chord. (Last release removed the footer `+ New task`; this puts the affordance back, in the header.)
