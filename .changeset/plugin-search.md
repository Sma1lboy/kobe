---
"@sma1lboy/kobe": patch
---

`kobe plugin search [query]` browses the marketplace from the CLI: one GitHub search over the `kobe-plugin` topic (sorted by stars) merged with the first-party examples, with an offline fallback to the first-party list.
