---
"@sma1lboy/kobe": patch
---

Internal: repo init prompt delivery now flows through a typed launch-time contract so engine session creation distinguishes repo first messages, explicit user prompts, and no automatic first message without ad hoc init-prompt suppression at each launch path.
