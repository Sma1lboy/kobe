---
"@sma1lboy/kobe": patch
---

Remove the desktop GUI chat shell and Agent Trace integration that was unintentionally merged into `main`. The experiment and its follow-up work remain available on the dedicated feature branch.
