---
"@sma1lboy/kobe": patch
---

Tab management is back on single-press chords: `ctrl+t` (new tab), `ctrl+w` (close tab), and `ctrl+]`/`ctrl+[` (next/previous tab) fire directly again, and their prefix strokes were removed — repeated actions like tab cycling don't suit a two-stroke sequence. The other prefix-moved rows stay prefix-only.
