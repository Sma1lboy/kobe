---
"@sma1lboy/kobe": patch
---

Web Board now renders the selected project's empty Kanban columns even when it has no issues yet, so new or empty projects still show the Backlog / In progress / Done board structure instead of a blank empty-state page.
