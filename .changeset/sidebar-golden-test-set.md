---
"@sma1lboy/kobe": patch
---

Golden test set for the sidebar, and a prune of the unit tests it supersedes. The sidebar's states are now locked as committed ground truth in two layers: `test/golden/sidebar-row-state.golden.txt` enumerates `buildSidebarRowView` over its full input space (every activity state × seen bit × worktree job × deletion phase × vendor × transcript, plus the engine-owned spinner frame sets, the `withSpinnerFrame` overlay contract, the turn-complete/transcript grace boundary, subagent marks, subtitle truncation, the PR check chip and the `tabRowActivity` truth table), and `test/render/golden/*.frame.txt` captures 17 whole OpenTUI frames of the real `SidebarTree` — resting/idle/running/complete/permission/rate-limited/error glyphs, non-agent tabs, pin + PR cluster, search pruning, archived view tabs, move mode, the recent-jump row, zen chip and the empty rail. Both regenerate with `KOBE_UPDATE_GOLDEN=1` and fail loudly on a missing golden, so a behavior change shows up as a reviewable diff instead of landing silently. With that in place, 44 test cases that sampled single rows of the same tables were removed (one whole file, `completion-still-working`) along with three assertions that only restated a constant's own literal; `row-view.ts` and `tree-core.ts` stay at 100% line coverage. No runtime behavior changes.
