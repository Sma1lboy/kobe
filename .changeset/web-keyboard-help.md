---
"@sma1lboy/kobe": patch
---

The web dashboard gains a keyboard-help overlay (the web counterpart of the TUI's F1 help): press `?` (when not typing in a field) or click the `?` in the top bar to see the shortcuts (Cmd/Ctrl+K command palette, Esc) and where the main affordances live (New task, Adopt, Overview, tab kinds, Notifications). The command palette also gained a footer hint (↑↓ move · ↵ run · esc close).
