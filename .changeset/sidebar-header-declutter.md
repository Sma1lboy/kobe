---
"@sma1lboy/kobe": patch
---

Sidebar header declutter: the `[/]` and `sort` chips are gone (search stays on `/`, sort stays on `t` — keyboard-only now), the PROJECTS row's filter label moved to the right end after the divider, and the task-count label was dropped.
