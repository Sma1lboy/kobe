---
"@sma1lboy/kobe": patch
---

fix: the Changes tab now collapses a fully-untracked directory into one muted row with a file count and summed line count (enter/click expands it into its files), instead of flooding the list with one `?` row per file and drowning the tracked changes.
