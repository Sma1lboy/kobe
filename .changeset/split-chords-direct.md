---
"@sma1lboy/kobe": patch
---

Split panes fire on their direct chords again — `ctrl+\` splits right, `ctrl+=` splits down. The prefix strokes (`ctrl+a \` / `ctrl+a =`) are dropped, same owner call as the tab-management and engine-picker rows.
