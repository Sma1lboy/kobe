---
"@sma1lboy/kobe": patch
---

The web dashboard's file diff view gains a "wrap" toggle: by default long diff lines scroll horizontally (so columns stay aligned), and one click soft-wraps them to fit the pane instead — handy for long strings, prose, or minified content. Per-file-preview, off by default.
