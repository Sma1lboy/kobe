---
"@sma1lboy/kobe": patch
---

Pressing Enter in the Overview filter now opens the top match (first card of the bucket walk — "needs you" first) and returns focus to the grid, matching the task rail's filter behavior. The full keyboard flow is now `/` → type → Enter, or `/` → type → Tab/j/k to browse the filtered grid.
