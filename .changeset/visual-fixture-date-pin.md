---
"@sma1lboy/kobe": patch
---

Fix the Kanban visual ground-truth gate breaking at midnight: issue `created` stamps can now be pinned via `KOBE_ISSUES_TODAY` (valid `YYYY-MM-DD` only, never set in production), and the visual fixture pins `2026-07-15` to match the committed screenshots.
