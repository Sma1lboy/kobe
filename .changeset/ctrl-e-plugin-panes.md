---
"@sma1lboy/kobe": patch
---

ctrl+e now lists installed plugin panes: the engine picker gains trailing rows for every enabled plugin's `[[panes]]` (shown by pane title — "Browser", "lazygit"); picking one opens it with the pane's placement (split beside the engine by default), no CLI needed. Pane launch composition is shared between the picker and `kobe plugin pane open`.
