---
"@sma1lboy/kobe": patch
---

Stop flashing a hollow-circle (`○`) status placeholder on freshly-spawned engine tabs. The turn glyph now renders only once the detector has a real reading, instead of defaulting to an "idle" circle in the window before the engine's native title arrives.
