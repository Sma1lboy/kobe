---
"@sma1lboy/kobe": patch
---

Add `kobe --skill` (and `kobe skill print`): print the bundled agent SKILL.md to stdout so a coding agent can learn the `kobe api` surface in one command, herdr-style — e.g. prompt an agent with ``read `kobe --skill` then fan out tasks`` without pre-installing the skill.
