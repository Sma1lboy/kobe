---
"@sma1lboy/kobe": patch
---

Add the `rove` CLI alongside the compatible `kobe` alias, accept `ROVE_*`
environment variables with precedence over `KOBE_*`, and continue using the
existing state paths without migration.
