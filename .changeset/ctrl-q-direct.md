---
"@sma1lboy/kobe": patch
---

`ctrl+q` is back as the direct "back to tasks" chord (`focus.sidebar`) — the escape hatch out of any pane is too load-bearing for a two-stroke prefix. Its prefix stroke is dropped along with the tab-management rows'.
