---
"@sma1lboy/kobe": patch
---

Story drawer: a linked story now shows an EVENTS section — the last 12 engine lifecycle events of its session (relative age, event kind, tool name / compaction trigger / subagent type, vendor), newest first. Fetched once when the drawer opens; a daemon with nothing recorded reads as "no engine events yet".
