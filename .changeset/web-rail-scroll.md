---
"@sma1lboy/kobe": patch
---

The task rail scrolls the selected task into view when it changes — so `j`/`k` keyboard navigation (or any selection) past the fold in a long task list keeps the active row visible instead of leaving it scrolled off-screen. No-op when the row is already visible.
