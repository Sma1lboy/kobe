---
"@sma1lboy/kobe": patch
---

Add an experimental `kobe-desktop` workspace: a thin Electron shell that launches the existing `kobe web` dev stack on a free local port block and opens it in a desktop window without changing the daemon, tmux, or web bridge architecture.
