---
"@sma1lboy/rove": patch
---

Make `@sma1lboy/rove` the canonical package for installs, workspace commands, update checks, and releases. Every release still publishes the identical build as `@sma1lboy/kobe`, and both `rove` and `kobe` commands remain available.
