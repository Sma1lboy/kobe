---
"@sma1lboy/kobe": patch
---

Removed the `● new` transcript-activity badge (Ops pane corner + files-column header) and its local fallback mtime poll. The daemon `transcript.activity` channel and the ChatTab "done" chip it feeds are unchanged.
