---
"@sma1lboy/kobe": patch
---

Fix the bottom-left HUD streaming `shift+p → shift+p` on every uppercase character typed into the embedded terminal — shift+letter chords are typing, not modifier chords, and no longer land HUD entries.
