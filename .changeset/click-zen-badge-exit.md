---
"@sma1lboy/kobe": patch
---

The `☯ ZEN` badge in the Tasks rail is now clickable — click it to exit zen mode, the mouse counterpart to the `prefix`+space chord. Since zen is global, exiting from the badge turns it off for every project.
