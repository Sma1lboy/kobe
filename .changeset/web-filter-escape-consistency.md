---
"@sma1lboy/kobe": patch
---

The web dashboard's Chat-transcript search and Changes-pane file filter now clear on Escape, matching the task rail and Overview filters — so Escape consistently empties whichever search/filter box is focused across the dashboard.
