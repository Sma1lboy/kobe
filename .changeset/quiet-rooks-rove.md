---
"@sma1lboy/kobe": patch
---

Make Rove the product name across the CLI, TUI, web dashboard, docs, website, agent skill, and generated brand assets while preserving the `kobe` command, packages, state paths, protocol fields, and plugin contracts for compatibility.
