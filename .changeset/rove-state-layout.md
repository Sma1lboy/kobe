---
"@sma1lboy/kobe": patch
---

Move new Rove product data, worktrees, branch names, and repo-init conventions to the Rove namespace. First launch copies supported `~/.kobe` and `~/.config/kobe` data without overwriting or deleting it; existing worktrees, branches, runtime processes, plugins, and `.kobe/init.*` files remain compatible.
