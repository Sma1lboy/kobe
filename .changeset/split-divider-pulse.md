---
"@sma1lboy/kobe": patch
---

feat: a just-created split leaf announces itself — its divider blinks twice in the focus accent (~0.5s) before settling, so ctrl+\ / ctrl+= give visible feedback about where the new pane landed. Chrome-only: the split geometry itself never animates.
