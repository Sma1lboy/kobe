---
"@sma1lboy/kobe": patch
---

Create PR in the Files pane moved from bare `p` to `ctrl+p` — lowercase letters stay reserved for frequent navigation; an action that fires a PR prompt earns a modifier.
