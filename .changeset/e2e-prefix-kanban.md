---
"@sma1lboy/kobe": patch
---

Fix the visual ground-truth e2e suite after the Kanban chord moved to `prefix+c`: the two Kanban journeys still opened the board with a bare `c`. No product behavior change — 0.8.36 was tagged but never published because this gate failed.
