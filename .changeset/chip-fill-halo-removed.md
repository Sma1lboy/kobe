---
"@sma1lboy/kobe": patch
---

fix: story-dialog chips (engine, after-start, session) drop their backgroundElement fill — border cells share the box background, so the fill haloed around the border line; selection now reads as primary border + bold text alone.
