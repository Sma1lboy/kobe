---
"@sma1lboy/kobe": patch
---

Move the "jump to next waiting task" chord off `ctrl+g` (which swallowed the engine's readline abort inside claude) onto `F7`, continuing kobe's F-row; `ctrl+g` now passes through to the engine again.
