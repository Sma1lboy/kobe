---
"@sma1lboy/kobe": patch
---

Give the scriptable `kobe api` surface its own documentation page instead of burying it inside the CLI reference, and link the published docs site from the README so it is discoverable at all.
