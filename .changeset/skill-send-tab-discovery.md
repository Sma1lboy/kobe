---
"@sma1lboy/kobe": patch
---

Document tab addressing in the kobe agent skill (v13): `send --tab tab-N`/`--tab new` existed in the API but the skill never mentioned it, and `kobe api inspect` is now called out as the way to enumerate a task's chat tabs before targeting one.
