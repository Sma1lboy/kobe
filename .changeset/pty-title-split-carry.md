---
"@sma1lboy/kobe": patch
---

Live tab titles no longer drop when a terminal's window-title escape splits across a PTY read boundary — the carry anchors on the OSC introducer again (the fix regressed during a module extraction).
