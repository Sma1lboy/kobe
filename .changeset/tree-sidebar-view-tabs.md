---
"@sma1lboy/kobe": patch
---

The Active/Archived row now really does stay hidden until something is archived. The gate shipped for the flat sidebar, but the tree sidebar — the default — renders its own copy of the tabs and kept showing them; `[`/`]` could also still cycle into an empty archive and strand you there. Both follow the same rule now.
