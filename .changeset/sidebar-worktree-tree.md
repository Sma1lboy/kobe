---
"@sma1lboy/kobe": patch
---

Sidebar as a tree: project → worktree → tab, with the right pane showing nothing but the active terminal. Tabs are rows under their worktree (only the selected one starts expanded), `enter` on a tab row switches straight to it, and the horizontal tab strip is retired by default (Settings → General → Terminal brings it back, or switch back to the flat sidebar there).
