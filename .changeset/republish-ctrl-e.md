---
"@sma1lboy/kobe": patch
---

Republish of the unreleased 0.8.27 (its CI publish was blocked by a flaky test-side race, now fixed): plugin panes in the ctrl+e picker — the engine picker lists every enabled plugin's `[[panes]]` after engines + shell, and picking one opens it with the pane's placement (split beside the engine by default).
