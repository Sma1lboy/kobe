---
"@sma1lboy/kobe": patch
---

Pane cycling prefix chords are now `prefix+h` (backward) and `prefix+l` (forward) instead of j/k — the three panes are laid out horizontally, so the left/right vim keys match the spatial direction. `F4` stays the direct forward alias; user YAML overrides still rebind either direction independently.
