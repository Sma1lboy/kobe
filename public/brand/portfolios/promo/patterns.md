# Promo Portfolio Patterns

patterns:
  - id: screen-first-field-scene
    rules:
      - use campaign brief and references as factual source
      - prefer product-forward promotional composition
      - keep the real kobe product surface dominant and readable
      - do not inherit release-note poster composition by default
  - id: mars-field-terminal-hero
    rules:
      - keep the Mars scene campaign-specific
      - preserve screen-first product hierarchy
  - id: voxel-field-terminal-hero
    rules:
      - keep the voxel scene campaign-specific
      - avoid official game marks
      - preserve kobe's terminal screen as the primary readable subject
